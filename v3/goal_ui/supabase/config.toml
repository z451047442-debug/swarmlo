# ⚠️ 安全提示：edge function 当前匿名可调用（verify_jwt = false）且无速率限制，
# 任何人可直接调用并消耗 AI_API_KEY 配额；CORS 亦为 *。公开部署前必须自行
# 加鉴权（verify_jwt = true + 前端带上 JWT）与限流，否则密钥配额可被刷爆。
project_id = "eqzmqxtngbulzhimrymr"

[functions.research-step]
verify_jwt = false

[functions.research-api]
verify_jwt = true