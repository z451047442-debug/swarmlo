# Structural smoke for the swarmlo-agent plugin (local WASM runtime — rvagent —
# plus the Anthropic Managed Agents cloud runtime, ADR-115). No network: it's
# a static check of the plugin manifest, skills, commands, MCP-tool references,
# and ADR cross-references. Triggers on changes to the plugin or this workflow.
name: swarmlo-agent-smoke

on:
  push:
    branches: [main, develop, v3]
    paths:
      - 'plugins/swarmlo-agent/**'
      - '.github/workflows/swarmlo-agent-smoke.yml'
  pull_request:
    branches: [main]
    paths:
      - 'plugins/swarmlo-agent/**'
      - '.github/workflows/swarmlo-agent-smoke.yml'
  workflow_dispatch:

jobs:
  smoke:
    name: swarmlo-agent structural smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run plugin smoke
        run: bash plugins/swarmlo-agent/scripts/smoke.sh
