# 复制为 .env 后填写（.env 已被 gitignore，不会误提交）。
# 值全部为占位符——从 src/ 前端代码与 supabase/functions/ 边缘函数实际读取的
# 变量推导而来；不要提交真实密钥。

# ---- 前端（Vite 前缀，构建期注入浏览器，仅放公开值）----
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# 可选：研究界面的默认 AI 模型（构建期注入；不设则默认 google/gemini-2.5-flash）
# VITE_AI_MODEL=deepseek-v4-pro

# ---- 边缘函数密钥（服务端专用，切勿出现在前端 bundle 中）----
# 配置位置：Supabase Dashboard → Project Settings → Edge Functions → Secrets
# 供所有函数使用：
# AI_BASE_URL=你的网关地址（不设默认 https://ai.gateway.lovable.dev/v1）
# AI_API_KEY=你的 AI 网关密钥（不设则回退到 LOVABLE_API_KEY）
# AI_MODEL=默认模型（不设默认 google/gemini-2.5-flash）
# AI_ENABLE_SEARCH=设为 1 时启用研究搜索（research-step 读取）
# LOVABLE_API_KEY=lovable-cloud 自动注入（自托管时手动填）

# 自托管 Supabase 时这些由函数运行时自动提供，无需手动设置：
# SUPABASE_URL
# SUPABASE_ANON_KEY
# SUPABASE_SERVICE_ROLE_KEY（保持机密）
# SUPABASE_DB_URL
